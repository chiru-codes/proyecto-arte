/* =============================================
   MÓDULO: GAME OVER
   Versión narrativa: deshabilitado.
   No hay pantalla de game over en la historia.
   Las consecuencias de no clickear las maneja
   la escena 4 directamente en game.js.
============================================= */
const GameOver = (() => {
    function show() {
        // No-op en modo historia
    }
    return { show };
})();
